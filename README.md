# Marki - Modern Documentation Platform

<div align="center">

**Marki** 是一个基于 React + TypeScript + Vite 的现代化文档平台，专为替代 GitBook 而设计。它可以作为静态站点生成器使用，也支持直接渲染远程 GitHub 仓库中的 Markdown 内容。

[![React](https://img.shields.io/badge/React-19.x-%2361DAFB?logo=react)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.x-%233178C6?logo=typescript)](https://www.typescriptlang.org/)
[![Vite](https://img.shields.io/badge/Vite-7.x-%23646CFF?logo=vite)](https://vitejs.dev/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

</div>

## ✨ 核心特性

### 🎯 双模式支持
- **📂 本地模式**：类似 GitBook，直接读取 `docs/` 目录下的 Markdown 文件
- **☁️ 远程模式**：通过配置，直接动态拉取 GitHub 仓库内容，无需重新构建即可更新内容

### 🎨 精致的 UI/UX
- **🌗 深色/浅色模式**：内置舒适的日间/夜间主题切换
- **📑 智能目录**：右侧 "On This Page" 目录支持滚动监听与高亮
- **📜 动态滚动条**：滚动时显示，停止滚动 1 秒后自动隐藏，视觉体验更流畅
- **📱 响应式设计**：完美适配移动端与桌面端
- **🔎 即时搜索**：支持文档标题快速检索
- **↔️ 上下文导航**：页脚自动生成"上一页/下一页"导航按钮

### 🔗 GitHub 深度集成
- **🕒 时间追踪**：自动获取并展示文档的最近更新时间和贡献者信息
- **🔗 外部链接识别**：智能识别外部链接并添加可视化标识
- **🖼️ 图片路径处理**：自动修正远程 Markdown 中的相对路径图片链接

### ⚙️ 高度可配置
- 通过 `public/settings.json` 即可实时修改网站标题、Logo、Favicon 及数据源仓库
- 支持本地/远程模式一键切换

## 🚀 快速开始

### 1️⃣ 安装依赖

```bash
npm install
```

### 2️⃣ 配置站点

在 [`public/settings.json`](public/settings.json) 中配置你的站点信息：

```json
{
  "mode": "local",                    // 模式选择："local" 或 "remote"
  "githubRepo": "Freakz3z/Marki",     // GitHub 仓库（远程模式需要）
  "branch": "main",                   // 分支名称
  "siteTitle": "Marki",               // 站点标题
  "navbarTitle": "Marki",             // 导航栏标题
  "logoPath": "/logo.jpg",            // Logo 路径
  "faviconPath": "/logo.jpg"          // Favicon 路径
}
```

#### 模式说明

**本地模式** (推荐用于开发/测试)
```json
{
  "mode": "local"
}
```
- 从项目的 `/docs` 目录加载文档
- 无需网络连接
- 无 CORS 问题

**远程模式** (推荐用于生产环境)
```json
{
  "mode": "remote",
  "githubRepo": "YourUsername/Your-Repo",
  "branch": "main"
}
```
- 从 GitHub 仓库动态加载文档
- 需要网络连接
- 更新内容无需重新构建

> 💡 **提示**：如果不设置 `mode` 字段，系统会自动判断：配置了 `githubRepo` 则使用远程模式，否则使用本地模式。

### 3️⃣ 启动开发服务器

```bash
npm run dev
```

访问 `https://localhost:5173`（项目已配置 HTTPS，避免 CORS 问题）

### 4️⃣ 构建生产版本

```bash
npm run build
```

构建产物位于 `dist/` 目录。

## 📦 部署

### 阿里云 ESA Pages

1. 在阿里云 ESA Pages 控制台创建新站点
2. 连接你的 GitHub 仓库
3. 配置构建设置：
   - **根目录**: `/`
   - **构建命令**: `npm run build`
   - **静态资源目录**: `dist`

### 其他静态托管平台

同样支持 Vercel、Netlify、Cloudflare Pages 等平台，配置与上述类似。

## 🛠️ 项目结构

```
Marki/
├── docs/                    # 本地模式文档目录
│   └── SUMMARY.md          # 文档导航结构
├── public/
│   ├── settings.json       # 运行时配置文件
│   └── ...                # 静态资源
├── src/
│   ├── components/         # React 组件
│   │   ├── MarkdownRenderer.tsx    # Markdown 渲染器
│   │   ├── Navbar.tsx              # 导航栏
│   │   ├── Sidebar.tsx             # 侧边栏
│   │   └── TableOfContents.tsx     # 目录组件
│   ├── layouts/
│   │   └── MainLayout.tsx   # 全局布局
│   ├── pages/              # 页面组件
│   ├── services/
│   │   ├── docs.ts         # 核心引擎（双模式切换、Markdown 解析）
│   │   └── config.ts       # 配置加载
│   ├── hooks/              # 自定义 Hooks
│   ├── store/              # 状态管理
│   └── types/              # TypeScript 类型定义
├── vite.config.ts          # Vite 配置（已启用 HTTPS）
└── package.json
```

## 🎨 主要功能详解

### 动态滚动条
- 滚动时自动显示滚动条
- 停止滚动 1 秒后逐渐隐藏
- 0.3s 平滑过渡动画
- 适用于侧边栏和主内容区域

### Markdown 支持
- **代码高亮**：基于 highlight.js 的语法高亮
- **GFM 支持**：GitHub Flavored Markdown
- **自定义组件**：支持自定义 React 组件嵌入

### 管理后台
访问 `/admin` 路径可以使用内置的简易 Markdown 编辑器（主要用于本地模式下的快速调试）。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

[MIT](LICENSE)

---

<div align="center">
 Made with ❤️ by <a href="https://github.com/Freakz3z">Freakz3z</a>
</div>

